{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://swarm.cloud/schemas/v1alpha1/publisher-secret.schema.json",
  "title": "PublisherSecret",
  "description": "Declares a secret the publishing organization provides in its admin console at publication time, referenced from the definition as {{ secrets.<name> }}. The definition never contains the value.",
  "type": "object",
  "required": ["name", "title"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-zA-Z][a-zA-Z0-9_]*$",
      "description": "Identifier used in the secrets.<name> expression namespace."
    },
    "title": {
      "type": "string",
      "description": "Label shown in the publication form."
    },
    "description": {
      "type": "string",
      "description": "Help text shown in the publication form."
    }
  }
}
